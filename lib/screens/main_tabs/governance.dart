import '../screen_barrel.dart';

class GovernanceScreen extends StatelessWidget {
  const GovernanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Governance: Upcoming Feature, still in development.",
        style: TextStyle(
          color: black,
        ),
      ),
    );
  }
}
